// import { Dimensions, PixelRatio } from 'react-native';
//
// const designWidth = 375;
// const designHeight = 812;
//
// const {
//   width: deviceWidth,
//   height: deviceHeight,
// } = Dimensions.get('window');
//
// const designDiagonal = Math.sqrt(designWidth ** 2 + designHeight ** 2);
// const deviceDiagonal = Math.sqrt(deviceWidth ** 2 + deviceHeight ** 2);
//
// const pixelRatio = deviceDiagonal / designDiagonal;

export const perfectSize = (size: number) => size;
// PixelRatio.roundToNearestPixel(size * pixelRatio);
