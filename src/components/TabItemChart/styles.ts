import { StyleSheet } from 'react-native';

import { mainColors } from 'constants/colors';

export const styles = StyleSheet.create({
  activeTab: {
    borderBottomWidth: 2,
    borderBottomColor: mainColors.blue,
    borderStyle: 'solid',
  },
});
